[project]
name = "integration"
version = "0.1.0"
requires-python = ">=3.14, <3.15"
dependencies = [
    "boto3==1.43.50",
    "requests==2.34.2",
    "pytest==9.1.1",
    "requests-aws4auth==1.3.2",
    "pytest-order==1.5.0",
]

[tool.uv]
package = false
